# For My Crush — simple webpage

Open `index.html` in a browser to view the page. For a local server (recommended) run:

```bash
# Python 3
python3 -m http.server 8000

# then open http://localhost:8000
```

Edit `styles.css` and `script.js` to customize messages and the phone number.
